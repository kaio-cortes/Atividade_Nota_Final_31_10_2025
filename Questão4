<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora de IMC</title>

    <style>

        body {
            font-family: Arial, sans-serif;
            background-color: #eef1f5; 
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .calculator-container {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 24px;
            width: 300px;
            text-align: center;
        }

        h2 {
            margin-top: 0;
            color: #333;
        }

        .input-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .input-group label {
            display: block;
            margin-bottom: 5px;
            color: #555;
            font-size: 0.9rem;
        }

        .input-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box; 
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #4CAF50; 
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #45a049; 
        }

        #resultado {
            margin-top: 20px;
            font-size: 1.1rem;
            font-weight: bold;
            color: #333

            height: 1.2em; 
        }

        #resultado.error {
            color: #e74c3c; 
        }
    </style>
</head>
<body>

    <div class="calculator-container">
        <h2>Calculadora de IMC</h2>
        
        <div class="input-group">
            <label for="peso">Peso (kg):</label>
            <input type="number" id="peso" placeholder="Ex: 70" step="0.1">
        </div>
        
        <div class="input-group">
            <label for="altura">Altura (m):</label>
            <input type="number" id="altura" placeholder="Ex: 1.75" step="0.01">
        </div>
        
        <button id="calcularBtn">Calcular IMC</button>
        
        <p id="resultado"></p>
    </div>

    <script>
       
        const pesoInput = document.getElementById('peso');
        const alturaInput = document.getElementById('altura');
        const calcularBtn = document.getElementById('calcularBtn');
        const resultadoElemento = document.getElementById('resultado');

       
            const peso = parseFloat(pesoInput.value);
            const altura = parseFloat(alturaInput.value);

            
            if (isNaN(peso) || isNaN(altura) || peso <= 0 || altura <= 0) {
                resultadoElemento.textContent = 'Por favor, insira valores válidos.';
                resultadoElemento.classList.add('error'); // Adiciona a classe de erro (vermelho)
                return; // Para a execução da função aqui
            }
)
            const imc = peso / (altura * altura);
            resultadoElemento.textContent = `Seu IMC é: ${imc.toFixed(2)}`;
            resultadoElemento.classList.remove('error'); // Remove a classe de erro, caso ela exista
        }

        calcularBtn.addEventListener('click', calcularIMC);

    </script>

</body>
</html>
